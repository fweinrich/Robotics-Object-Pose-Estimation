# source global ros
$ source /opt/ros/noetic/setup.bash

# create a catkin workspace
$ mkdir -p catkin_ws/src && cd catkin_ws

# clone the driver
$ git clone https://github.com/UniversalRobots/Universal_Robots_ROS_Driver.git src/Universal_Robots_ROS_Driver

# clone fork of the description. This is currently necessary, until the changes are merged upstream.
$ git clone -b calibration_devel https://github.com/fmauch/universal_robot.git src/fmauch_universal_robot

# install dependencies
$ sudo apt update -qq
$ rosdep update
$ rosdep install --from-paths src --ignore-src -y

# build the workspace. We need an isolated build because of the non-catkin library package.
$ catkin_make

# activate the workspace (ie: source it)
$ source devel/setup.bash


roslaunch ur_calibration calibration_correction.launch \
  robot_ip:=app target_filename:="${HOME}/ur5_calibration.yaml"

  roslaunch ur_robot_driver ur5_bringup.launch robot_ip:=docker.for.mac.localhost


roslaunch ur_robot_driver ur5_bringup.launch robot_ip:=app kinematics_config:=/root/ur5_calibration.yaml

http://moveit2_tutorials.picknik.ai/doc/getting_started/getting_started.html

sudo apt install python3-wstool
apt install ros-noetic-graph-msgs ros-noetic-roslint